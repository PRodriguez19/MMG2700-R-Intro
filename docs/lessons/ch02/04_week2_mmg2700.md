# Lesson 4: Introduction to R Data Structures 

## Learning Objectives

* Describe the biological question addressed by the Blackmore et al. study.
* Describe what a data.frame is.
* Load external data from a .csv file into a data frame.
* Summarize the contents of a data frame.
* Describe what a factor is.
* Convert between strings and factors.
* Reorder and rename factors.
* Export and save data.

***

# Getting set up

+ Set the working directory to the `bioc-intro` folder made in the previous class 

+ Download CSV file

+ Creating code chunk with `Command+Option+I`

```r
download.file(url = "https://github.com/carpentries-incubator/bioc-intro/raw/main/episodes/data/rnaseq.csv",
              destfile = "data/rnaseq.csv")
```

You are now ready to load the data:

```r
rna <- read.csv("data/rnaseq.csv") # comma separated values
```

This statement doesn’t produce any output because, as you might recall, assignments don’t display anything. If we want to check that our data has been loaded, we can see the contents of the data frame by typing its name:

```r
#View(rna)
```

+ Let's take a moment to really dig into this.
+ The first column contains HGNC gene symbols. These are the gene names you are probably most familiar with. However, there are several other gene identifier systems that are commonly used in bioinformatics pipelines.
+ The third column contains the GEO sample-specific accession number. This is a unique identifier assigned to each sample in the Gene Expression Omnibus (GEO). When we deposit NGS data into GEO, each sample is assigned its own accession number.
+ The other columns I want to point out are the Ensembl ID and Entrez ID columns. These are two commonly used systems for identifying genes. Entrez Gene IDs are maintained by NCBI, while Ensembl IDs are provided by the Ensembl project at EMBL-EBI.
+ You might think that we could easily convert one gene ID system to another, but it isn't always that simple. Different databases use different annotation methods, algorithms, and criteria. As a result, one Entrez ID does not always map perfectly to a single Ensembl ID, and some identifiers may have multiple mappings or no current mapping at all.

## Why is this important? 

Gene names are for humans; gene IDs are for databases and computational pipelines—and moving between those systems is not always straightforward.


# What are data frames?

+ We previously used the `str` command. This allowed us to identify the structure of an R object. 

```r
str(rna) #structure of R object 
```

This data frame was created once we imported the csv file. So what is it? 

Data frames are the *de facto* data structure for most tabular data, and what we use for statistics and plotting.

A data frame can be created by hand, but most commonly they are generated by the functions `read.csv()` or `read.table()`; in other words, when importing spreadsheets from your hard drive (or the web).

In genomics, we work with a lot of tabular data - data organized in rows and columns. The data structure that stores this type of data is a data frame. Data frames are collections of vectors of the same length but can be of different types. Because we often have data of multiple types, it is natural to examine that data in a data frame.

<figure markdown="span">
  ![data frame](../../img/data-frame.svg){ width="400" }
</figure>

## Why not just work in Microsoft Excel? 

You may be tempted to open and manually work with these data in excel. However, there are a number of reasons why this can be to your detriment. First, it is very easy to make mistakes when working with large amounts of tabular data in excel. Have you ever mistakenly left out a column or row while sorting data? Second, many of the files that we work with are so large (big data) that excel and your local machine do not have the bandwidth to handle them. Third, you will likely need to apply analyses that are unavailable in excel. Lastly, it is difficult to keep track of any data manipulation steps or analyses in a point and click environment like excel. 

R, on the other hand, can make analyzing tabular data more efficient and reproducible. But before getting into working with this data in R, let's review some best practices for data management. 

## Inspecting `data.frames`

We already saw how the functions `head()` and `str()` can be useful to check the content and the structure of a data frame. Here is a non-exhaustive list of functions to get a sense of the content/structure of the data. 

Size:

  + `dim(rna)` - returns a vector with the number of rows as the first element, and the number of columns as the second element (the dimensions of the object).
  + `nrow(rna)` - returns the number of rows.
  + `ncol(rna)` - returns the number of columns.

Content:

  + `head(rna)` - shows the first 6 rows.
  + `tail(rna)` - shows the last 6 rows.

Names:

  + `names(rna)` - returns the column names (synonym of colnames() for data.frame objects).
  + `rownames(rna)` - returns the row names.

Summary:

  + `str(rna)` - structure of the object and information about the class, length and content of each column.
  + `summary(rna)` - summary statistics for each column.


```r
dim(rna)
```


```r
#names(rna)
```

```r
nrow(rna)
```


```r
#rownames(rna)
```

```r
rna <- data.frame(rna)
```


## Indexing and subsetting data frames

Our rna data frame has rows and columns (it has 2 dimensions); if we want to extract some specific data from it, we need to specify the “coordinates” we want. Row numbers come first, followed by column numbers. However, note that different ways of specifying these coordinates lead to results with different classes.

```r
# object[row,columns]
rna[1,1] # the first element in the first column 

# Extract the 2 column only
rna[, 2]
```

```
# Extract the first 5 rows and output to `head_rna`
rna5 <- rna[1:5, ]
```

The `:` is a special function that creates numeric vectors of integers.  

!!! example "Class Exercise 1" 

    We need the first three elements in the 7th column. Redirect the output to `rna7`. 


You can also exclude certain indices of a data frame using the "-" sign: 

```r
rna[, -1] # contains the whole data frame, except the first column
```

### The core idea of subsetting

+ From this object, give me these parts 
+ What goes inside the brackets depends on the type of data 
+ Currently, we are subsetting based on rows and columns

```r
#data[rows, columns]
```

!!! example "Class Exercise 2" 

    1. Create a data.frame (rna_200) containing only the data in row 200 of the rna dataset.

    2. Notice how `nrow()` gave you the number of rows in a data.frame?

      + Use that number to pull out just that last row in the initial `rna` data frame.
      + Compare that with what you see as the last row using `tail()` to make sure it’s meeting expectations.
      + Pull out that last row using `nrow()` instead of the row number.
      + Create a new data frame called `rna_last` from that last row.


## Subsetting rows using values in a column 

What happens if we want a specific gene of interest? 

First, we can access a single column: 

```r
rna$gene 
```

This selects the gene column from the `rna` data frame.

Now, we can ask a question about the values in that column:

```r
rna$gene == "Asl" 
```

This does not return the gene data itself. Instead, it returns a logical vector containing `TRUE` or `FALSE` for each row.

TRUE = the gene in that row is "Asl"
FALSE = the gene in that row is something else

We can use this logical vector to keep only the rows where the condition is TRUE:

```r
rna[rna$gene == "Asl", ] # keep only the rows which are TRUE
```

This means:

Keep the rows where rna$gene is "Asl", and the blank after the comma means *keep all columns*.

Remember that the general syntax for subsetting a data frame is:

```
data[rows, columns]
```

!!! example "Class Exercise 3"

    Modify the code above so that you: 
    
    + Keep only the first 6 columns
    + Save the result as a new object called `rna_asl`
    + View the object  


## Adding multiple conditions with `&`

Sometimes we want to select rows that meet more than one condition.

For example, we might want to find the gene Asl, but only in samples from the cerebellum.

To require that both conditions are `TRUE`, we use the `&` operator:


```r
rna$gene == "Asl" & rna$tissue == "Cerebellum" # Is the gene Asl AND is the tissue Cerebellum?
```

The `&` operator means AND.

Both conditions must be `TRUE` for a row to be selected.

!!! example "Class Exercise 4" 

    Modify the code so that you select rows where: 
    
    + gene = `"Asl"`
    + sex = `Female` 
    + output to `rna_asl_sex`

    How many rows did you get?


## Matching multiple values with `%in%`

What if we are interested in more than one gene?

We could use multiple == statements, but you will see this becomes difficult to read when we have many values.

```
rna$gene == "Kif5a" |
rna$gene == "Kif21b" |
rna$gene == "Kif14"
```

Instead, we can use `%in%`. 

```r
#%in% 

#membership testing; each value is tested against the guest list
```

R checks every value in rna$gene against the list provided. If the gene is on the list, R returns `TRUE`. If it is not, R returns `FALSE`.


!!! example "Class Exercise 5"  

    Your favorite genes are Kinesin family members: `Kif5a`, `Kif21b`, and `Kif14`.

    Select only the rows where:

      + `gene` is `Kif5a`, `Kif21b`, or `Kif14`
      + `infection` = `"NonInfected"`
      + Save the result as `rna_kif`

    Then view your new object. How many rows are in this new object? 


## Summary of Commands 

```{r}
# commands 
# `&` - AND, both conditions must be `TRUE`
# `%in%` - Is this value in my list? 
```

### Citation

To cite material from this course in your publications, please use:

Some materials used in these lessons were derived from work that is Copyright © Data Carpentry (http://datacarpentry.org/). 
All Data Carpentry instructional material is made available under the [Creative Commons Attribution license](https://creativecommons.org/licenses/by/4.0/) (CC BY 4.0).

Other materials were derived from Babraham Bioinformatics - Training Courses. (n.d.). https://www.bioinformatics.babraham.ac.uk/training.html  

NIH Bioinformatics Training and Education Program. "Introductory R for Novices". Last updated January 2026. https://bioinformatics.ccr.cancer.gov/docs/r_for_novices/

---








